import React from "react";
import Typography from "@material-ui/core/Typography";

const Title = () => (
  <Typography
    color="primary"
    variant="h2"
    style={{
      marginTop: 15,
      marginBottom: 15,
      textAlign: "center"
    }}
  >
    Book Search
  </Typography>
);

export default Title;
