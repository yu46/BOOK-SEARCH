import React from "react";
import Typography from "@material-ui/core/Typography";

const Title = () => (
  <Typography
    color="primary"
    variant="h2"
    style={{
      margin: "25px auto",
      textAlign: "center",
      fontWeight: "bold"
    }}
  >
    Book Search
  </Typography>
);

export default Title;
